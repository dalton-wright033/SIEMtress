[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "siemtress"
version = "0.1.0"
description = "A modular Python log parsing and analysis utility"

[project.scripts]
siemtress = "siemtress.main:main"